<title>Show</title>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Pricing</th>
    <th>Category</th>
    <th>Action</th>
  </tr>
    <tr>
      <td><%= @products.name %></td>
      <td><%= @products.description %></td>
      <td><%= @products.pricing %></td>
      <td><%= @category.name %></td>
      <td><a href="/products/<%= @products.id %>/edit">Edit</a>&nbsp<a href="/back">Back</a></td>
    </tr>

</table>
<form class="" action="/comments" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  Comment:
  <input type="text" name="comment" value="">
  <input type="submit" name="submit" value="submit">
</form>
